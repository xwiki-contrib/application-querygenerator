application-querygenerator
==========================

Generates XWQL queries and UI from XClasses

The Query Generator uses XWiki's core capabilities to generate search forms and a query automatically. 
